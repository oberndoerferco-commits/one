---
description: Publish the next piece from the journal backlog
---

Publish the next journal piece.

`editorial-director`:

1. Query the `made-in-italy` journal for pieces with `isPublished: false` —
   there are ten finished and waiting.
2. Pick the one with the strongest purchase intent behind its subject: a
   question someone asks before spending money on something we actually make.
3. Read it against `CLAUDE.md` — the register, and the retire list.
4. Verify every factual claim. Anything unsourced comes out or gets flagged to
   `atelier-director` to substantiate.
5. Confirm it links to at least two live pieces it genuinely serves.
6. Check the meta title and description read like a house, not a keyword string.

Then `creative-director` approves or sends it back with the reason.

Show me the piece and what changed — **then wait for my go-ahead** before it
publishes. Afterwards log it to `docs/handbook/content-log.md` with the date and
target phrase, so we can measure it at 90 days.

One piece, then stop. Spaced out, not a burst.
